# 链表

带头结点的单链表。核心功能有按值按位查找、按位插入、按位删除、头插、尾插

单链表操作的**“黄金法则”**：

>  当需要对第 `i` 个节点进行操作时（插入或删除），必须先拿到第 `i-1` 个节点（前驱节点）的指针

因此容易想到把“获取指定位置的节点的指针”进行封装，即“按位查找”函数 getElem()

## 代码实现

~~~cpp
#include <stdio.h>
#include <stdlib.h>

// 1. 数据结构定义
typedef int elemType;

// 单链表节点定义
typedef struct LNode {
    elemType data;         // 数据域
    struct LNode *next;    // 指针域
} LNode, *LinkList; 
// 提示：LinkList 等价于 LNode*，为了代码可读性：
// 当强调这是一个单链表（如头指针）时，用 LinkList
// 当强调这是一个普通节点时，用 LNode*

// ==================== 核心操作函数框架 ====================

// 1. 初始化（带头结点）
// 要求：分配一个头结点，其数据域不存实际数据，next 指针置为 NULL。
bool initList(LinkList &L) {
    L = (LNode*)malloc( sizeof(LNode)*1 );
    if(L==NULL){
        return false;
    }
    L->next = NULL;    
    return true;
}

// 2. 判空
// 要求：判断单链表是否为空表（除头结点外没有任何元素）。为空返回 true。
bool isEmpty(LinkList L) {
    return L->next==NULL;
}

// 3. 求表长
// 要求：统计单链表中有效数据节点的个数（不包含头结点）。
int listLength(LinkList L) {
    int length = 0;
    while(L->next!=NULL){
        length += 1;
    }
    return length;
}

// 4. 按位查找
// 要求：返回第 index 个位置的节点指针。如果 i=0，返回头结点；如果 index 越界，返回 NULL。
LNode* getElem(LinkList L, int index) {
    LNode* p = L;
    for(int i=0;i<index&&p!=NULL;i++){
        p = p->next;
    }
    return p;
}

// 5. 按值查找
// 要求：查找第一个数据域等于 e 的节点，并返回其指针。找不到返回 NULL。
LNode* locateElem(LinkList L, elemType e) {
    LNode* p = L;
    while(p!=NULL){
        p = p->next;
        if(p->data==e){
            return p;
        }
    }
    return NULL;
}

// 6. 按位插入
// 要求：在第 index 个位置（1 <= index <= length+1）插入新元素 e。成功返回 true，越界或内存分配失败返回 false。
bool listInsert(LinkList &L, int index, elemType e) {
    if( !(index>=1&&index<=listLength(L)+1) ){
        return false;
    }
    
    LNode* newNode = (LNode*)malloc( sizeof(LNode)*1 );
    if(newNode==NULL){
        return false;
    }
    newNode->data = e;
    
    LNode* p, t;
    p = getElem(L, index-1);  // 调用getElem()省时省力
    t = p->next;
    
    p->next = newNode;
    newNode->next = t;
    return true;    
}

// 7. 按位删除
// 要求：删除第 index 个位置的节点（1 <= index <= length），并将被删节点的数据存入 e 中返回。成功返回 true，越界返回 false。
bool listDelete(LinkList &L, int index, elemType &e) {
    if( !(index>=1&&index<=listLength(L)) ){
        return false;
    }
    
    LNode* p, t;
    p = getElem(L, i-1);
    t = p->next;
    
    e = t->data;
    p->next = t->next;
    free(t);
    return true;
}

// 8. 遍历打印
// 要求：从第一个有效节点开始，依次打印各节点的数据。
void printList(LinkList L) {
    LNode* p = L;
    while(p->next!=NULL){
        p = p->next;
        printf("%d ",p->data);
    }
}

// 9. 销毁单链表
// 要求：释放链表中所有节点（包括头结点）占用的内存空间，防止内存泄漏，最后将头指针置空。
void destroyList(LinkList &L) {
    LNode* p = L;
    LNode* t = L->next;
    while(t!=NULL){
        p = t;
        t = t->next;
        free(p);
    }
    free(p);
    L->next = NULL;
}

// ==================== 408 常考专项练习 ====================

// 10. 头插法建立单链表
// 要求：不断读取用户输入（假设固定插入一个数组的数据），将新节点永远插在头结点之后。
// 提示：这种方法建好的链表，数据顺序与插入顺序是相反的（逆置）。
void list_HeadInsert(LinkList &L, elemType arr[], int n) {
    for(int i=0;i<n;i++){
        listInsert(L, 1, arr[i]);
    }
}

// 11. 尾插法建立单链表
// 要求：不断读取用户输入（假设固定插入一个数组的数据），将新节点永远插在链表的最后面。
// 提示：需要维护一个尾指针（尾后指针），保证每次插入的时间复杂度为 O(1)。
void list_TailInsert(LinkList &L, elemType arr[], int n) {
    for(int i=0;i<n;i++){
        listInsert(L, listLength(L)+1, arr[i]);
    }
}

// 主函数：单链表测试
int main() {
    LinkList L;
    
    printf("========== 1. 初始化与空表测试 ==========\n");
    initList(L);
    printf("空表状态 isEmpty (期望输出 1): %d\n", isEmpty(L));
    printf("空表长度 listLength (期望输出 0): %d\n", listLength(L));

    printf("\n========== 2. 尾插法建表与基础插入 ==========\n");
    // 测试尾插法：顺序应该与数组一致
    int arr1[] = {10, 20, 30};
    list_TailInsert(L, arr1, 3);
    printf("尾插法建表后内容 (期望输出 10 20 30): ");
    printList(L);
    
    // 测试常规插入与头部插入
    listInsert(L, 1, 99); // 头插
    listInsert(L, 5, 88); // 尾插 (当前长度4，插在第5个位置)
    printf("头尾插入后内容 (期望输出 99 10 20 30 88): ");
    printList(L);
    printf("当前表长 (期望输出 5): %d\n", listLength(L));

    printf("\n========== 3. 越界插入防守测试 ==========\n");
    printf("越界插入 i=0 (期望输出 0): %d\n", listInsert(L, 0, 77));
    printf("越界插入 i=7 [当前len=5] (期望输出 0): %d\n", listInsert(L, 7, 77));

    printf("\n========== 4. 删除操作边界测试 ==========\n");
    int e = 0;
    // 测试删除头部
    listDelete(L, 1, e);
    printf("删除第1个节点，弹出的值为 (期望 99): %d\n", e);
    
    // 测试删除尾部
    listDelete(L, listLength(L), e);
    printf("删除末尾节点，弹出的值为 (期望 88): %d\n", e);
    
    printf("删头去尾后内容 (期望输出 10 20 30): ");
    printList(L);

    printf("\n========== 5. 越界删除防守测试 ==========\n");
    printf("越界删除 i=0 (期望输出 0): %d\n", listDelete(L, 0, e));
    printf("越界删除 i=4 [当前len=3] (期望输出 0): %d\n", listDelete(L, 4, e));

    printf("\n========== 6. 查找与指针越界测试 ==========\n");
    LNode* p = getElem(L, 2);
    if (p != NULL) printf("按位查找 i=2 (期望输出 20): %d\n", p->data);

    // 408 常见陷阱：按位查找 i=0 必须返回头结点指针，不能是 NULL
    p = getElem(L, 0);
    printf("按位查找 i=0 是否返回头结点 (期望输出 1): %d\n", p == L);
    
    p = getElem(L, 100);
    printf("按位查找严重越界 i=100 是否返回 NULL (期望输出 1): %d\n", p == NULL);

    p = locateElem(L, 30);
    if (p != NULL) printf("按值查找存在的 30，是否成功获取指针 (期望输出 1): %d\n", p != NULL);

    p = locateElem(L, 1000);
    printf("按值查找不存在的 1000，是否返回 NULL (期望输出 1): %d\n", p == NULL);

    printf("\n========== 7. 头插法逆置与销毁测试 ==========\n");
    destroyList(L); // 先把前面的表销毁，防止内存泄漏
    printf("原表销毁完成。尝试测试头插法...\n");

    LinkList L2;
    initList(L2);
    int arr2[] = {1, 2, 3, 4, 5};
    // 测试头插法：顺序应该与数组完全相反（逆置特性，408极爱考）
    list_HeadInsert(L2, arr2, 5);
    printf("头插法建表后内容 (期望输出 5 4 3 2 1): ");
    printList(L2);

    destroyList(L2);
    printf("\n终极销毁完成，所有测试结束！\n");

    return 0;
}
~~~

