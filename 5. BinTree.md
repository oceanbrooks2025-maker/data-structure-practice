# 二叉树

树形结构大量用到了**递归**思想。

树形结构的表示：扩展序列 (前序遍历)

`AB#D##C##`

~~~
	  A
    /   \
   B     C
    \
     D
~~~





## 代码实现

~~~cpp
#include <stdio.h>
#include <stdlib.h>

typedef char elemType;

// 二叉树节点定义
typedef struct BiTNode {
    elemType data;                  // 数据域
    struct BiTNode *lchild, *rchild;// 左右孩子指针
} BiTNode, *BiTree;

// ==================== 核心操作函数框架 ====================

// 1. 前序遍历递归建树 (核心难点)
// 要求：读取由 '#' 表示空节点的扩展前序序列来建立二叉树。
// 提示：注意这里的参数 char* &str，它是“指针的引用”。
// 这意味着在递归中 str++ 向后移动时，所有的递归层都能看到 str 移动后的位置。
bool CreateBiTree(BiTree &T, char* &str) {
    // 【请在此处填入你的代码】
    // 逻辑提示：
    // 1. 如果当前字符是 '\0'，直接返回 false。
    // 2. 如果当前字符是 '#'，说明是空节点，T 置为 NULL，str指针后移，返回 true。
    // 3. 如果是普通字符，分配内存给 T，赋值 data。
    // 4. 递归创建左子树 CreateBiTree(T->lchild, str)。
    // 5. 递归创建右子树 CreateBiTree(T->rchild, str)。
    if(*str=='\0'){
        return false;
    }
    else if(*str=='#'){
        T = NULL;
        str++;
        return true;
    }
    else{
        T = (BiTNode*)malloc( sizeof(BiTNode) );
        if(T==NULL){
            return false;
        }
        
        T->data = *str;
        str++;
        CreateBiTree(T->lchild, str);
        CreateBiTree(T->rchild, str);
        return true;
    }
    
}

// 2. 前序遍历 (PreOrder)
// 要求：根 -> 左 -> 右
void PreOrder(BiTree T) {
    if(T==NULL){
        return ;
    }
    else{
        printf("%c ", T->data);
        PreOrder(T->lchild);
        PreOrder(T->rchild);
    }
}

// 3. 中序遍历 (InOrder)
// 要求：左 -> 根 -> 右
void InOrder(BiTree T) {
    if(T==NULL){
        return ;
    }
    else{
		InOrder(T->lchild);
        printf("%c ", T->data);
        InOrder(T->rchild);
    }
}

// 4. 后序遍历 (PostOrder)
// 要求：左 -> 右 -> 根
void PostOrder(BiTree T) {
    if(T==NULL){
        return ;
    }
    else{
        PostOrder(T->lchild);
        PostOrder(T->rchild);
		printf("%c ", T->data);
    }
}

// 5. 求二叉树深度 (🚨 408 极高频考点)
// 要求：返回整棵树的深度（层数）。
// 提示：空树深度为0；非空树的深度 = max(左子树深度, 右子树深度) + 1。
int TreeDepth(BiTree T) {
    if(T==NULL){
		return 0;
	}
    else{
        int l = TreeDepth(T->lchild);
        int r = TreeDepth(T->rchild);
        return ((l>r)?l:r) + 1;
    }
}

// 6. 销毁二叉树
// 要求：释放所有节点内存。
// 提示：必须用【后序遍历】的思路！为什么不能用前序？想想看。
// 核心原因是父节点必须最后释放，左右顺序不重要
void DestroyBiTree(BiTree &T) {
    if(T==NULL){
        return ;
    }
    else{
        DestroyBiTree(T->lchild);
        DestroyBiTree(T->rchild);
		free(T);
        T=NULL;
    }
}
~~~



## 测试

~~~cpp
// ==================== 测试模块 ====================
int main() {
    int TEST_CREATE = 1; // 建树与前序遍历测试
    int TEST_TRAVERSAL = 0; // 中序、后序与深度测试
    int TEST_DESTROY = 0; // 销毁测试

    BiTree T = NULL;
    
    // 我们用来测试的扩展前序序列: 
    // 对应的树形结构：
    //       A
    //     /   \
    //    B     C
    //   / \     \
    //  D   E     F
    char treeStr[] = "ABD##E##C#F##";
    char *p = treeStr; // 用指针 p 来辅助建树遍历字符串

    if (TEST_CREATE) {
        printf("========== 1. 建树与前序遍历测试 ==========\n");
        CreateBiTree(T, p);
        printf("建树完成。\n");
        
        printf("前序遍历 (期望: A B D E C F): ");
        PreOrder(T);
        printf("\n");
    }

    if (TEST_TRAVERSAL) {
        printf("\n========== 2. 其他遍历与深度测试 ==========\n");
        printf("中序遍历 (期望: D B E A C F): ");
        InOrder(T);
        printf("\n");

        printf("后序遍历 (期望: D E B F C A): ");
        PostOrder(T);
        printf("\n");

        printf("二叉树深度 (期望: 3): %d\n", TreeDepth(T));
    }

    if (TEST_DESTROY) {
        printf("\n========== 3. 销毁二叉树测试 ==========\n");
        DestroyBiTree(T);
        printf("销毁完毕。(期望T为空: %d)\n", T == NULL);
    }

    return 0;
}
~~~





## 层序遍历

从“深度优先遍历”切换到“广度优先遍历”

需要用到队列，暂存右节点指针

核心思想一言以蔽之就是“父节点出队，同时子节点入队”

~~~cpp
#include <stdio.h>
#include <stdlib.h>

#define MAX_SIZE 50 // 队列容量稍微开大一点，防止溢出

typedef char elemType;

// 1. 二叉树节点定义
typedef struct BiTNode {
    elemType data;
    struct BiTNode *lchild, *rchild;
} BiTNode, *BiTree;

// 2. 循环队列定义 (🚨 注意：这里的数据类型变成了 BiTree 树指针)
typedef struct {
    BiTree data[MAX_SIZE]; 
    int front; 
    int rear;  
} SqQueue;

// ==================== 队列标准操作 (已为你铺垫好) ====================
bool initQueue(SqQueue &Q) { Q.front = Q.rear = 0; return true; }
bool isEmpty(SqQueue Q) { return Q.front == Q.rear; }
bool isFull(SqQueue Q) { return (Q.rear + 1) % MAX_SIZE == Q.front; }
bool enQueue(SqQueue &Q, BiTree e) {
    if (isFull(Q)) return false;
    Q.data[Q.rear] = e;
    Q.rear = (Q.rear + 1) % MAX_SIZE;
    return true;
}
bool deQueue(SqQueue &Q, BiTree &e) {
    if (isEmpty(Q)) return false;
    e = Q.data[Q.front];
    Q.front = (Q.front + 1) % MAX_SIZE;
    return true;
}

// ==================== 二叉树辅助操作 ====================
// 递归建树（已优化为指针解引用风格）
bool CreateBiTree(BiTree &T, char* &str) {
    if (*str == '\0') return false;
    if (*str == '#') {
        T = NULL;
        str++;
        return true;
    }
    T = (BiTNode*)malloc(sizeof(BiTNode));
    T->data = *str;
    str++;
    CreateBiTree(T->lchild, str);
    CreateBiTree(T->rchild, str);
    return true;
}

void DestroyBiTree(BiTree &T) {
    if (T == NULL) return;
    DestroyBiTree(T->lchild);
    DestroyBiTree(T->rchild);
    free(T);
    T = NULL;
}

// ==================== 核心操作函数框架 ====================

// 层序遍历 (广度优先 BFS)
// 逻辑提示：
// 1. 如果树为空，直接 return。
// 2. 初始化一个队列 Q。
// 3. 将根节点 T 入队。
// 4. 当队列不为空时，循环执行：
//    a. 出队一个节点 p。
//    b. 访问（打印）节点 p 的数据。
//    c. 如果 p 有左孩子，左孩子入队。
//    d. 如果 p 有右孩子，右孩子入队。
void LevelOrder(BiTree T) {
	if(T==NULL){
		return ;
	}
    SqQueue Q;
    initQueue(Q);
    enQueue(Q, T);
    while( !isEmpty(Q) ){
    	BiTNode *p = NULL;
    	deQueue(Q, p);
    	printf("%c ", p->data);
		
        if( p->lchild!=NULL ){
        	enQueue(Q, p->lchild);
        }
        
        if( p->rchild!=NULL ){
        	enQueue(Q, p->rchild);
        }
    }
}


// ==================== 测试模块 ====================
int main() {
    BiTree T = NULL;
    
    // 依然使用我们熟悉的测试树：
    //       A
    //     /   \
    //    B     C
    //   / \     \
    //  D   E     F
    char treeStr[] = "ABD##E##C#F##";
    char *p = treeStr; 
    
    printf("========== 1. 层序遍历测试 ==========\n");
    CreateBiTree(T, p);
    printf("建树完成。\n");
    
    printf("层序遍历 (期望输出: A B C D E F): ");
    LevelOrder(T);
    printf("\n");
    
    DestroyBiTree(T);
    return 0;
}
~~~



## 非递归的中序遍历

在走到最左端之前，借助栈结构暂存所有沿途的节点

核心思路是：

还可以往左，就压栈+往左走

左边没有了，退一步+打印+往右

~~~cpp
#include <stdio.h>
#include <stdlib.h>

typedef char elemType;

// 二叉树节点
typedef struct BiTNode {
    elemType data;
    struct BiTNode *lchild, *rchild;
} BiTNode, *BiTree;

// ==================== 链栈标准操作 (已铺垫) ====================
// 🚨 注意：栈里存的是树节点指针 BiTree
typedef struct LinkNode {
    BiTree data;
    struct LinkNode *next;
} LinkNode, *LiStack;

bool initStack(LiStack &S) { S = NULL; return true; }
bool stackEmpty(LiStack S) { return S == NULL; }
bool push(LiStack &S, BiTree e) {
    LinkNode* newNode = (LinkNode*)malloc(sizeof(LinkNode));
    if (!newNode) return false;
    newNode->data = e;
    newNode->next = S;
    S = newNode;
    return true;
}
bool pop(LiStack &S, BiTree &e) {
    if (S == NULL) return false;
    e = S->data;
    LinkNode *t = S;
    S = S->next;
    free(t);
    return true;
}

// ==================== 树的基础辅助 ====================
bool CreateBiTree(BiTree &T, char* &str) {
    if (*str == '\0') return false;
    if (*str == '#') {
        T = NULL;
        str++;
        return true;
    }
    T = (BiTNode*)malloc(sizeof(BiTNode));
    T->data = *str;
    str++;
    CreateBiTree(T->lchild, str);
    CreateBiTree(T->rchild, str);
    return true;
}

void DestroyBiTree(BiTree &T) {
    if (T == NULL) return;
    DestroyBiTree(T->lchild);
    DestroyBiTree(T->rchild);
    free(T);
    T = NULL;
}

// ==================== 核心操作函数框架 ====================

// 中序非递归遍历 (左 -> 根 -> 右)
// 逻辑提示：
// 1. 初始化一个空栈 S。
// 2. 设立一个指针 p 指向根节点 T。
// 3. 当 p 不为空 或者 栈不为空时，循环执行：
//    a. 如果 p 不为空：说明还有左孩子可以找，将 p 压栈，然后 p = p->lchild 一路向左。
//    b. 如果 p 为空：说明左边走到头了。此时出栈一个节点，打印它，然后 p = 这个节点的右孩子 (准备探索右路)。
void InOrderNonRecursive(BiTree T) {
    if( T==NULL ){
        return ;
    }
    
    BiTNode *p = T;
    LiStack S;
    initStack(S);
    while( p!=NULL || !stackEmpty(S) ){
        if( p!=NULL ){
            push(S, p);
            p = p->lchild;
        }
        else{
            pop(S, p);
            printf("%c ",p->data);
            p = p->rchild;
        }
    }
}

// ==================== 测试模块 ====================
int main() {
    BiTree T = NULL;
    
    // 测试树：
    //       A
    //     /   \
    //    B     C
    //   / \     \
    //  D   E     F
    char treeStr[] = "ABD##E##C#F##";
    char *strPtr = treeStr; 
    
    printf("========== 中序非递归遍历测试 ==========\n");
    CreateBiTree(T, strPtr);
    
    printf("期望输出: D B E A C F\n");
    printf("实际输出: ");
    InOrderNonRecursive(T);
    printf("\n");
    
    DestroyBiTree(T);
    return 0;
}
~~~





### 非递归的后序遍历





~~~cpp
#include <stdio.h>
#include <stdlib.h>

typedef char elemType;

// ==================== 1. 结构体定义 ====================
// 二叉树节点
typedef struct BiTNode {
    elemType data;
    struct BiTNode *lchild, *rchild;
} BiTNode, *BiTree;

// 链栈节点 (栈内存储二叉树指针)
typedef struct LinkNode {
    BiTree data;
    struct LinkNode *next;
} LinkNode, *LiStack;

// ==================== 2. 栈操作 (已为你铺垫) ====================
bool initStack(LiStack &S) { S = NULL; return true; }
bool stackEmpty(LiStack S) { return S == NULL; }
bool push(LiStack &S, BiTree e) {
    LinkNode* newNode = (LinkNode*)malloc(sizeof(LinkNode));
    if (!newNode) return false;
    newNode->data = e;
    newNode->next = S;
    S = newNode;
    return true;
}
bool pop(LiStack &S, BiTree &e) {
    if (S == NULL) return false;
    e = S->data;
    LinkNode *t = S;
    S = S->next;
    free(t);
    return true;
}
// 🚨 专门为后序遍历准备的“只看不弹”
bool getTop(LiStack S, BiTree &e) {
    if (S == NULL) return false;
    e = S->data;
    return true;
}

// ==================== 3. 树操作辅助 ====================
bool CreateBiTree(BiTree &T, char* &str) {
    if (*str == '\0') return false;
    if (*str == '#') { T = NULL; str++; return true; }
    T = (BiTNode*)malloc(sizeof(BiTNode));
    T->data = *str; str++;
    CreateBiTree(T->lchild, str);
    CreateBiTree(T->rchild, str);
    return true;
}
void DestroyBiTree(BiTree &T) {
    if (T == NULL) return;
    DestroyBiTree(T->lchild);
    DestroyBiTree(T->rchild);
    free(T);
    T = NULL;
}

// ==================== 4. 核心挑战 ====================

// 后序非递归遍历 (左 -> 右 -> 根)
void PostOrderNonRecursive(BiTree T) {
    LiStack S;
    initStack(S);
    
    BiTNode *p = T;    // 游标指针：负责探路
    BiTNode *r = NULL; // 辅助指针：记录最近一次被访问（打印）的节点
    
    // 【请在此处填入你的核心循环代码】
    // 逻辑提示：
    // while (p != NULL || !stackEmpty(S)) {
    //     1. 只要 p 不为空，把 p 压栈，然后 p = p->lchild 一路向左走到黑。
    //     2. 如果 p 为空，说明左边走不通了。此时【读取栈顶】给 p (用 getTop，千万别 pop)。
    //     3. 检查 p 的右孩子：
    //        如果 p 有右孩子，并且这个右孩子【不是】r：
    //            说明右子树还没走过，此时 p = p->rchild，准备去探索右边。
    //        否则（无右孩子，或者右孩子就是 r）：
    //            说明左右两边都搞定了。
    //            此时真正【pop】出栈给 p。
    //            打印 p->data。
    //            将 r 更新为 p (标记它为刚刚被打印过的节点)。
    //            最关键的一步：将 p 置为 NULL (防止下一轮循环再把它错误地压栈，强迫下一轮去查看新的栈顶)。
    // }
    while( p!= NULL||!stackEmpty(S) ){
        // 一路往左走，沿途节点压栈
        if( p!=NULL ){  
            push(S, p);
        	p = p->lchild;
        }
        // 到最左边了，回头
        else{
            getTop(S, p);
            // 先走右节点，如果右节点不空且没走过
            if( p->rchild!=NULL&&p->rchild!=r ){
                p = p->rchild;
            }
            // 右节点走不了了，才打印根节点
            else{
                pop(S, p);
                printf("%c ", p->data);
                r = p;
                p = NULL;
            }
            
        }
    }
}

// ==================== 5. 测试模块 ====================
int main() {
    BiTree T = NULL;
    
    // 测试树：
    //       A
    //     /   \
    //    B     C
    //   / \     \
    //  D   E     F
    char treeStr[] = "ABD##E##C#F##";
    char *strPtr = treeStr; 
    
    printf("========== 后序非递归遍历测试 ==========\n");
    CreateBiTree(T, strPtr);
    
    printf("期望输出: D E B F C A\n");
    printf("实际输出: ");
    PostOrderNonRecursive(T);
    printf("\n");
    
    DestroyBiTree(T);
    return 0;
}
~~~







~~~~

~~~~

