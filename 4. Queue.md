## 队列

最基础的队列（链式）也就是限制只能在表尾进行操作的链表，结构上来说只需要增加一个队尾指针，进队出队操作都借助这个指针即可。

这里我们直接跳过基础队列，进入到顺序循环队列。

### 顺序循环队列

普通的数组队列存在“假溢出”问题，即多次进队出队后，头尾指针都跑到后面去了，导致前面区域被空置无法使用。为了解决这个问题，我们需要在逻辑上把数组卷起来，变成一个“环”。具体执行的时候，实际上是借助了**取模运算** `%` 

此外，为了方便判空判满，通常采用“牺牲一个存储单元”的策略。



#### 实现

```cpp
#include <stdio.h>
#include <stdlib.h>

#define MAX_SIZE 5 // 为了方便测试“满队”状态，故意把容量设得很小

typedef int elemType;

// 循环队列结构体定义
typedef struct {
    elemType data[MAX_SIZE];
    int front; // 队头指针：指向队头元素
    int rear;  // 队尾指针：指向队尾元素的【下一个】位置
} SqQueue;

// ==================== 核心操作函数框架 ====================

// 1. 初始化
// 要求：初始化空队列，front 和 rear 都置为 0。
bool initQueue(SqQueue &Q) {
    Q.front = 0;
    Q.rear = 0;
    return true;
}

// 2. 判空
// 要求：当 front 和 rear 相等时，队列为空。
bool isEmpty(SqQueue Q) {
    return Q.front==Q.rear;
}

// 3. 判满 (🚨 408 核心考点)
// 要求：采用“牺牲一个存储单元”的方法。当 rear 的下一个位置是 front 时，判定为满。
// 提示：绝对不能用 rear + 1 == front，必须使用取模运算 % MAX_SIZE 把数组卷成环！
bool isFull(SqQueue Q) {
    return ( (Q.rear+1)%MAX_SIZE ) == Q.front;
}

// 4. 入队 (EnQueue)
// 要求：如果队列未满，将元素 e 存入 rear 指向的位置，然后 rear 后移一位（注意取模）。
bool enQueue(SqQueue &Q, elemType e) {
    if( isFull(Q) ){
        return false;
    }
    
    Q.data[Q.rear] = e;
    Q.rear = (Q.rear+1) % MAX_SIZE;
    return true;
}

// 5. 出队 (DeQueue)
// 要求：如果队列不空，将 front 指向的元素用 e 返回，然后 front 后移一位（注意取模）。
bool deQueue(SqQueue &Q, elemType &e) {
    if(isEmpty(Q)){
        return false;
    }
    
    e = Q.data[Q.front];
    Q.front = (Q.front+1) % MAX_SIZE;
    return false;
}

// 6. 获取队列有效元素个数 (🚨 408 常考小题)
// 要求：利用 front, rear 和 MAX_SIZE 计算当前队列里有几个元素。
// 提示：(rear + MAX_SIZE - front) % MAX_SIZE。想想为什么？
int queueLength(SqQueue Q) {
    // 不清晰 先分情况讨论
    // if( Q.rear<Q.front ){
    //     return Q.rear+MAX_SIZE-Q.front;
    // }
    // else{
    //     return Q.rear-Q.front;
    // }
	
	// 整合为一个式子
	return (Q.rear+MAX_SIZE-Q.front)%MAX_SIZE;
}
```



#### 测试

```cpp
// ==================== 测试模块 ====================
int main() {
    SqQueue Q;
    
    printf("========== 1. 初始化测试 ==========\n");
    initQueue(Q);
    printf("队列判空 (期望 1): %d\n", isEmpty(Q));
    
    printf("\n========== 2. 入队与判满测试 ==========\n");
    enQueue(Q, 11);
    enQueue(Q, 22);
    enQueue(Q, 33);
    enQueue(Q, 44);
    // MAX_SIZE 是 5，牺牲一个单元后，最多只能装 4 个元素
    printf("装入4个元素后，判满 isFull (期望 1): %d\n", isFull(Q));
    printf("此时强行入队 (期望 0): %d\n", enQueue(Q, 55));
    printf("当前队列长度 (期望 4): %d\n", queueLength(Q));
    
    printf("\n========== 3. 循环出入队测试 (检验模运算) ==========\n");
    int e;
    deQueue(Q, e); printf("出队元素 (期望 11): %d\n", e);
    deQueue(Q, e); printf("出队元素 (期望 22): %d\n", e);
    
    printf("出队两次后，队列长度 (期望 2): %d\n", queueLength(Q));
    printf("出队两次后，判满 isFull (期望 0): %d\n", isFull(Q));
    
    printf("重新入队两个元素 66, 77 (检验rear指针是否成功绕回数组头部)...\n");
    enQueue(Q, 66);
    enQueue(Q, 77);
    printf("再次判满 isFull (期望 1): %d\n", isFull(Q));
    
    printf("\n========== 4. 清空队列测试 ==========\n");
    deQueue(Q, e); printf("出队 (期望 33): %d\n", e);
    deQueue(Q, e); printf("出队 (期望 44): %d\n", e);
    deQueue(Q, e); printf("出队 (期望 66): %d\n", e);
    deQueue(Q, e); printf("出队 (期望 77): %d\n", e);
    
    printf("全部出队后，判空 isEmpty (期望 1): %d\n", isEmpty(Q));
    printf("空队强行出队 (期望 0): %d\n", deQueue(Q, e));

    return 0;
}
```



