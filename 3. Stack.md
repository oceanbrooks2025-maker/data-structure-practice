## 栈

链栈，即所有操作（压栈、弹栈）只能在头结点进行的链表。一般不带头结点，此时栈指针直接指向第一个节点。

### 实现代码

~~~cpp
#include <stdio.h>
#include <stdlib.h>

typedef int elemType;

// 链栈节点定义（和单链表一模一样）
typedef struct LinkNode {
    elemType data;
    struct LinkNode *next;
} LinkNode, *LiStack; 
// 提示：S 即为栈顶指针，指向栈顶第一个元素。S == NULL 即为空栈。

// ==================== 核心操作函数框架 ====================

// 1. 初始化
// 要求：初始化一个空栈（不带头结点）。
bool initStack(LiStack &S) {
    // 不理解为什么要有返回值？
    S = NULL;
    return true;
}

// 2. 判空
// 要求：判断栈是否为空。
bool stackEmpty(LiStack S) {
    return S==NULL;
}

// 3. 进栈 (Push)
// 要求：将元素 e 压入栈顶。本质上就是“不带头结点的单链表头插法”。
// 提示：需要单独申请新节点，并更新栈顶指针 S。
bool push(LiStack &S, elemType e) {
    LinkNode* newNode = (LinkNode*)malloc(sizeof(LinkNode));
    if( newNode==NULL ){
        return false;
    }
    
    newNode->data = e;
    newNode->next = S;
    S = newNode;
    return true;
}

// 4. 出栈 (Pop)
// 要求：将栈顶元素弹出，用 e 返回其值，并释放节点内存。
// 🚨 防守重点：空栈绝对不能出栈（会引发空指针解引用）。
bool pop(LiStack &S, elemType &e) {
    if(S==NULL){
        return false;
    }
    else{
        e = S->data;
        LinkNode *t = S;
        S = S->next;
        free(t);
        return true;
    }
}

// 5. 读取栈顶元素 (GetTop)
// 要求：读取栈顶元素的值并用 e 返回，但不删除该节点。
bool getTop(LiStack S, elemType &e) {
    if(S==NULL){
        return false;
    }
    else{
        e = S->data;
        return true;
    }
}

// 6. 从顶到底打印栈内容（非标准操作，仅为调试方便）
// 提示：沿着 next 指针一路打印即可。
void printStack(LiStack S) {
    LinkNode *p = S;
    while(p!=NULL){
        printf("%d ", p->data);
        p = p->next;
    }
    printf("\n");
}

// 7. 销毁栈
// 要求：清空并释放栈内所有节点。
void destroyStack(LiStack &S) {
    while(S!=NULL){
        LinkNode *t = S->next;
        free(S);
        S = t;
    }
}
~~~



### 测试

~~~cpp
// ==================== 测试模块 ====================

int main() {
    // 🛠️ 测试开关：完成对应函数后，改为 1 启用测试
    int TEST_INIT  = 1; // 初始化测试
    int TEST_PUSH  = 1; // 进栈测试
    int TEST_POP   = 1; // 出栈与读取测试
    
    LiStack S;
    
    if (TEST_INIT) {
        printf("========== 1. 初始化测试 ==========\n");
        if (initStack(S)) {
            printf("初始化: 成功 (注意此时 S 应为 NULL)\n");
            printf("空栈状态 stackEmpty (期望 1): %d\n", stackEmpty(S));
        } else {
            printf("初始化: 失败\n");
        }
    }

    if (TEST_PUSH) {
        printf("\n========== 2. 进栈 (Push) 测试 ==========\n");
        push(S, 10);
        push(S, 20);
        push(S, 30);
        printf("连续压入 10, 20, 30。\n");
        
        // 栈的特性是 LIFO (后进先出)，所以打印结果应该是逆序的
        printf("当前栈内容 (期望从顶到底为: 30 20 10): ");
        printStack(S);
        printf("进栈后空栈状态 (期望 0): %d\n", stackEmpty(S));
    }

    if (TEST_POP) {
        printf("\n========== 3. 出栈 (Pop) 与读取测试 ==========\n");
        int e = 0;
        
        // 测试获取栈顶
        getTop(S, e);
        printf("读取栈顶元素 (期望 30): %d\n", e);
        
        // 测试正常出栈
        pop(S, e);
        printf("弹出栈顶元素 (期望 30): %d\n", e);
        printf("弹栈后当前内容 (期望: 20 10): ");
        printStack(S);
        
        // 测试连续出栈直至空栈
        pop(S, e); // 弹出 20
        pop(S, e); // 弹出 10
        printf("连续两次弹栈后空栈状态 (期望 1): %d\n", stackEmpty(S));
        
        // 🚨 终极边界测试：空栈强行弹栈或读取
        printf("空栈强行出栈 (期望 0): %d\n", pop(S, e));
        printf("空栈强行读取 (期望 0): %d\n", getTop(S, e));
    }

    if (TEST_INIT) {
        printf("\n========== 4. 销毁测试 ==========\n");
        destroyStack(S);
        printf("销毁完毕 (S指针状态 期望为空: %d)\n", S == NULL);
    }

    return 0;
}
~~~

