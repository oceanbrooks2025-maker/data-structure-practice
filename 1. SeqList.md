# 编程基础
## cpp基础操作
~~~cpp
#include <iostream>
using namespace std;
int main() {
    int a=0;
    cin>>a;
    cout<<a;
    return 0;
}
~~~
## 类定义
~~~ cpp
class Student {
private:   // 成员变量（属性）
    string name;
    int age;

public:  // 成员函数（方法）
    void setInfo(string n, int a) {
        name = n;
        age = a;
    }

    void showInfo() {
        cout << name << " " << age << endl;
    }
}; 
~~~
## 类成员函数定义
类内定义
~~~cpp
class A {
public:
    void func() {
        cout << "hello";
    }
};
~~~
类内声明+类外定义
~~~cpp
class A {
public:
    void func(); 
};
void A::func() {
    cout << "hello";
}
~~~
## 构造&析构函数
~~~cpp
class Person {
public:
    Person() {}
    Person(string n) {
        name = n;
    }
    
    ~Person() {
        cout << "对象销毁了\n";
    }

private:
    string name;
};
~~~

# 顺序表（c++类实现）

目标数据结构：顺序表

描述：空间可动态调整

期望功能：创建 销毁 增删改查 是否为空 长度 打印

## 源码

~~~cpp
#include <iostream>
#include <cstdlib>
using namespace std;

class SeqList {
private: 
    int capacity = 0;
    int length = 0;
    int* element ;

public: 
    SeqList(int n=10) {
        if(n<1){
            n=10;
        }
        capacity = n;
        length = 0;
        element = (int *)malloc( sizeof(int)*capacity );
    };

    int getlen(){ return length;};
    int getCap(){ return capacity;};
    void printList(){
        for(int i=0;i<length;i++){
            cout<<element[i]<<" ";
        }
        cout<<endl;
    };
    bool insert(int index, int num);
    bool remove(int index);
    bool replace(int index, int num);
    bool getElem(int index, int &num);
    int searchElem(int num);
    bool inEmpty();
    bool expand(int new_capacity);
    
    ~SeqList() {
        free(element);
    };
    
}; 

bool SeqList::insert(int index, int num){
    if(index<1||index>length+1){
        return false;
    }
    if(length>=capacity){
        return false;
    }

    for(int i=length-1;i>=index-1;i--){
        element[i+1] = element[i];
    }
    element[index-1]=num;
    length += 1;
    return true;
}

bool SeqList::remove(int index){
    if(index<1 || index>length){
        return false;
    }
    for(int i=index-1;i<=length-2;i++){
        element[i] = element[i+1];
    }
    length -= 1;
    return true;
}  

bool SeqList::replace(int index, int num){
    if(index<1 || index>length){
        return false;
    }
    element[index-1] = num;
    return true;
}

bool SeqList::getElem(int index, int &num){
    if(index>0&&index<length+1){
        num = element[index-1];
		return true;
    }
	return false;
}

int SeqList::searchElem(int num){
    for(int i=0;i<length;i++){
        if(element[i]==num){
            return i+1;
        }
    }
    return -1;
}

bool SeqList::inEmpty(){
    return length==0;
}

bool SeqList::expand(int new_capacity){
    int *new_element = (int *)malloc( sizeof(int)*new_capacity);
    if(!new_element){
        return false;
    }
    
    for(int i=0;i<length;i++){
        new_element[i]=element[i];
    }
    free(element);
    element = new_element;
	capacity = new_capacity;
    return true;
}


int main() {
    SeqList L(11);
    for(int i=0;i<10;i++){
        L.insert(i+1, i+1);    
    }
    L.printList();
	
	cout<<L.getCap()<<endl;
	L.expand(20);
	cout<<L.getCap()<<endl;
	L.printList();

    return 0;
}
~~~



## 边界测试

~~~
int main() {
    cout << "========== 1. 初始化与空表边界测试 ==========\n";
    // 测试初始化异常值（传入0或负数）
    SeqList L1(0); 
    cout << "传入0时，初始容量 (期望输出 10): " << L1.getCap() << endl;
    cout << "空表状态是否正确 (期望输出 1): " << L1.inEmpty() << endl;
    // 空表时强行删除或获取
    cout << "空表删除头部 (期望输出 0): " << L1.remove(1) << endl;
    int tempVal = 0;
    cout << "空表获取元素 (期望输出 0): " << L1.getElem(1, tempVal) << endl;

    cout << "\n========== 2. 插入操作边界测试 ==========\n";
    SeqList L(3); // 故意设置极小容量，方便快速测试满载情况
    // 越界插入测试
    cout << "下界越界插入 index=0 (期望输出 0): " << L.insert(0, 100) << endl;
    cout << "上界越界插入 index=2 [当前len=0] (期望输出 0): " << L.insert(2, 100) << endl;
    
    // 正常填充至满载
    L.insert(1, 10); // 插入第一项: [10]
    L.insert(1, 20); // 头插测试: [20, 10]
    L.insert(3, 30); // 尾插测试: [20, 10, 30]
    cout << "当前列表情况: "; 
    L.printList();
    
    // 满表后继续插入
    cout << "满表强行插入 (期望输出 0): " << L.insert(4, 40) << endl;

    cout << "\n========== 3. 扩容与满表后续测试 ==========\n";
    cout << "尝试扩容至 5 (期望输出 1): " << L.expand(5) << endl;
    cout << "扩容后再次尾插 index=4 (期望输出 1): " << L.insert(4, 40) << endl;
    cout << "当前列表情况 (期望输出 20 10 30 40): "; 
    L.printList();

    cout << "\n========== 4. 删除操作边界测试 ==========\n";
    // 越界删除测试
    cout << "下界越界删除 index=0 (期望输出 0): " << L.remove(0) << endl;
    cout << "上界越界删除 index=5 [当前len=4] (期望输出 0): " << L.remove(5) << endl;
    
    // 头尾删除测试
    cout << "删除尾部 index=4 (期望输出 1): " << L.remove(4) << endl;
    cout << "删除头部 index=1 (期望输出 1): " << L.remove(1) << endl;
    cout << "当前列表情况 (期望输出 10 30): "; 
    L.printList();

    // 连续删除至清空
    L.remove(1);
    L.remove(1);
    cout << "清空后检查 inEmpty (期望输出 1): " << L.inEmpty() << endl;
    cout << "被掏空后再强行删除 (期望输出 0): " << L.remove(1) << endl;

    cout << "\n========== 5. 查找与替换边界测试 ==========\n";
    // 重新准备数据 [99, 88, 77]
    L.insert(1, 99);
    L.insert(2, 88);
    L.insert(3, 77); 

    // 获取元素越界测试
    cout << "获取越界 index=0 (期望输出 0): " << L.getElem(0, tempVal) << endl;
    cout << "获取越界 index=4 (期望输出 0): " << L.getElem(4, tempVal) << endl;
    cout << "正常获取 index=2 (期望输出 1): " << L.getElem(2, tempVal) << "，提取的值: " << tempVal << endl;

    // 搜索测试
    cout << "搜索不存在的元素 100 (期望输出 -1): " << L.searchElem(100) << endl;
    cout << "搜索存在的元素 77 (期望输出 3): " << L.searchElem(77) << endl;

    // 替换越界测试
    cout << "替换越界 index=4 (期望输出 0): " << L.replace(4, 66) << endl;
    cout << "正常替换头部 index=1 (期望输出 1): " << L.replace(1, 66) << endl;
    cout << "最终列表情况 (期望输出 66 88 77): "; 
    L.printList();

    return 0;
}
~~~



# 顺序表（混编风格）

在应试场景下，还是遵循参考资料的风格比较好（混编风格，主体用c实现，例如结构体、全局函数；但是允许用c++的引用）

## 源码

主体用c的结构体+全局函数实现，传参用到了c++的引用

允许自定义元素类型；自动动态扩容

~~~cpp
#include <stdio.h>
#include <stdlib.h>

#define INIT_CAPACITY 8

typedef int elemType;

typedef struct {
    elemType* data;
    int length;
    int capacity;
} SeqList;

bool listInit(SeqList &L){
    L.data = (elemType*)malloc( sizeof(elemType)*INIT_CAPACITY );
    if(L.data == NULL){
        return false;
    }
    
    L.length = 0;
    L.capacity = INIT_CAPACITY;
    return true;
}

bool isEmpty(SeqList L){
    return L.length==0;
}

bool listExpand(SeqList &L){
    // 将容量扩充1倍
    elemType* newData = (elemType*)malloc( sizeof(elemType)*L.capacity*2 );
    if(newData == NULL){
        return false;
    }
    
    for(int i=0;i<L.length;i++){
        newData[i] = L.data[i];
    }
    
    free(L.data);
    L.data = newData;
    L.capacity *= 2;
    return true;
}

void listPrint(SeqList L){
    for(int i=0;i<L.length;i++){
        // 存疑：如何打印自定义类型
        // 回复：非考生考虑范围
        printf("%d ",L.data[i]);
    }    
}

bool listInsert(SeqList &L, int index, elemType newElem){
    if( !(index>=1&&index<=L.length+1) ){
        return false;
    }
    
    // 容量不够了就扩容
    if(L.length>=L.capacity){
        bool succeeded = listExpand(L);
		if( succeeded==false ){
			return false;
		}
    }
	
    // 插入点后面的元素 往后挪 顺序从后往前
    for(int i=L.length-1;i>=index-1;i--){
        L.data[i+1] = L.data[i];
    }
    L.data[index-1] = newElem;
    
    L.length += 1;
    return true;    
}

bool listDelete(SeqList &L, int index){
    if( !(index>=1&&index<=L.length) ){
        return false;
    }
    
    // 删除点后面的元素 往前挪 顺序从前往后
    for(int i=index-1;i<L.length;i++){
        L.data[i] = L.data[i+1];
    }
	L.length -= 1;
    return true;    
}

bool listReplace(SeqList L, int index, elemType newElem){
    if(index>=1&&index<=L.length){
        L.data[index-1] = newElem;
        return true;
    }
    return false;    
}

bool getElem(SeqList L, int index, elemType &result){
    if(index>=1 && index<=L.length){
        result = L.data[index-1];
        return true;
    }
    return false;
}

bool searchElem(SeqList L, elemType target, int &index){
    for(int i=0;i<L.length;i++){
        if(L.data[i]==target){
            index = i+1;
            return true;
        }
    }
    index = -1;
    return false;
}

void listDestroy(SeqList &L){
    if(L.data!=NULL){
        free(L.data);
        L.data = NULL;
        L.length = 0;
        L.capacity = 0;
    }
}

int main(){
    return 0;
}
~~~



## 测试

~~~cpp
int main() {
    SeqList L;
    
    printf("========== 1. 初始化与空表边界测试 ==========\n");
    listInit(L);
    printf("初始容量 (期望输出 8): %d\n", L.capacity);
    printf("空表状态isEmpty (期望输出 1): %d\n", isEmpty(L));

    printf("\n========== 2. 插入与自动扩容边界测试 ==========\n");
    // 初始容量是8，故意插入9个元素以触发自动扩容
    for(int i = 1; i <= 9; i++) {
        listInsert(L, i, i * 10); // 插入 10, 20, 30 ... 90
    }
    printf("插入9个元素后容量 (期望输出 16): %d\n", L.capacity);
    printf("当前列表内容: ");
    listPrint(L);
    printf("\n");
    
    printf("越界插入 index=0 (期望输出 0): %d\n", listInsert(L, 0, 99));
    printf("越界插入 index=11 [当前len=9] (期望输出 0): %d\n", listInsert(L, 11, 99));

    printf("\n========== 3. 删除边界测试 ==========\n");
    listDelete(L, 1); // 删除头部
    printf("删头后内容 (期望首位是20): ");
    listPrint(L);
    printf("\n");
    
    listDelete(L, L.length); // 删除尾部
    printf("删尾后内容 (期望末位是80): ");
    listPrint(L);
    printf("\n");

    printf("越界删除 index=0 (期望输出 0): %d\n", listDelete(L, 0));
    printf("越界删除 index=9 [当前len=7] (期望输出 0): %d\n", listDelete(L, 9));

    printf("\n========== 4. 查找与替换测试 ==========\n");
    listReplace(L, 1, 999);
    printf("替换头部后: ");
    listPrint(L);
    printf("\n");

    int targetIndex = 0;
    searchElem(L, 80, targetIndex);
    printf("搜索存在的元素 80 (期望输出 7): %d\n", targetIndex);
    
    searchElem(L, 1000, targetIndex);
    printf("搜索不存在的元素 1000 (期望输出 -1): %d\n", targetIndex);

    printf("\n========== 5. 销毁测试 ==========\n");
    listDestroy(L);
    printf("销毁完成。\n");

    return 0;
}
~~~













































